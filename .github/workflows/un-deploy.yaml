name: Un-deploy with Terragrunt

on: workflow_dispatch

jobs:
  un-deploy-infra:
    permissions:
      contents: read
      pull-requests: read
      id-token: write
    uses: devops-homelab/homelab-github-reusable-workflows/.github/workflows/tf-un-deploy.yaml@main
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      DO_ACCESS_TOKEN: ${{ secrets.DO_ACCESS_TOKEN }}
    with:
      config_file: config.env